# Opportunity Scanner (self-evolution loop) runtime config
#
# Notes:
# - This file is an example. Do not put secrets here.
# - DeepSeek is intentionally forbidden for Opportunity Scanner LLM calls.

# Enable/disable the scanner step.
SPARK_OPPORTUNITY_SCANNER=1

# Force the LLM provider used by the scanner ("minimax" recommended).
SPARK_OPPORTUNITY_LLM_PROVIDER=minimax

# Run an LLM "deep scan" at most once every 15 minutes per session (900s),
# but only when there is enough meaningful prompt/edit context.
SPARK_OPPORTUNITY_LLM_COOLDOWN_S=900

# Minimum combined context length required before the scanner will call the LLM.
SPARK_OPPORTUNITY_LLM_MIN_CONTEXT_CHARS=140

# Per-provider timeout budget (seconds). MiniMax is higher latency than local.
SPARK_OPPORTUNITY_LLM_TIMEOUT_S=12

# Bound work per bridge cycle (max events scanned from the recent backlog).
SPARK_OPPORTUNITY_SCAN_EVENT_LIMIT=120
