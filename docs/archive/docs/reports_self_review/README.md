# Archived Advisory Self-Reviews

This folder stores historical advisory self-review reports moved out of `docs/reports/` root to reduce operational noise.

Rules:
- Keep latest quick-access self-reviews in `docs/reports/`.
- Move older repetitive self-review logs here using:
  - `python scripts/archive_self_reviews.py --apply --keep-latest 3`
